apply plugin: 'java'
apply plugin: 'eclipse'

repositories
{
	mavenCentral()
}

dependencies
{
	compile group: 'com.google.guava', name: 'guava', version: '17.0'
	compile rootProject
}

libsDirName = '../../build/dist'

artifacts
{ archives jar }